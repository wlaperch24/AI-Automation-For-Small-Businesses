{
  "name": "missed-call-sms-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel-only missed-call SMS booking workflow with OpenPhone, Google Sheets, and Google Calendar",
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "luxon": "^3.5.0",
    "nodemailer": "^6.10.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/nodemailer": "^6.4.17",
    "@vercel/node": "^5.0.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
